services:
  reloj-detector:
    build: .
    container_name: reloj-detector
    restart: unless-stopped
    ports:
      - "127.0.0.1:8080:8080"
    env_file:
      - .env
    deploy:
      resources:
        limits:
          memory: 1536M
